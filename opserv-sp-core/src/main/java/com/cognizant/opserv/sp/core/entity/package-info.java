
package com.cognizant.opserv.sp.core.entity;
/*@TypeDefs(
{
	
	@TypeDef (name="encryptedString", typeClass=StringEncryptorImpl.class,
		parameters= {
			@Parameter(name="encryptorRegisteredName",  value="hibernateStringEncryptor")
		}
	),	
	@TypeDef (name="encryptedDate", typeClass=DateEncryptorImpl.class,
		parameters= {
			@Parameter(name="encryptorRegisteredName",  value="hibernateDateEncryptor")
		}
	),	
	@TypeDef (name="encryptedDecimal", typeClass=BigDecimalEncryptorImpl.class,
		parameters= {
			@Parameter(name="encryptorRegisteredName",  value="hibernateDecimalEncryptor")
		}
	),	
	@TypeDef (name="encryptedInteger", typeClass=BigIntEncryptorImpl.class,
		parameters= {
			@Parameter(name="encryptorRegisteredName",  value="hibernateIntegerEncryptor")
		}
	)
})




import org.hibernate.annotations.Parameter;
import org.hibernate.annotations.TypeDef;
import org.hibernate.annotations.TypeDefs;
//import org.jasypt.hibernate4.type.EncryptedStringType;

*/